import { ProductoData } from "@/app/types/types"

export const realizarVenta = (carrito: ProductoData [], recargos:number, descuentos:number) => {
    // 1 - Crear model 
    
        // Pedido ={
        //     listaProducto : carrito,
        //     total: (carrito.total + recargos) - descuentos

        // }


        // 2- const respuestaStripe = postCharges(pedidoModel)


        // 3- Guardar veta en DB
        // if(respuestaStripe.status === 200){
        // 3- postPedidoDB(pedidoModel)

        // }

        //fijarse si implementar lo de payment o lo de charges
    


}